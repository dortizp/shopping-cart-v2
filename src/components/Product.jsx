const Product = ({title, description, img, price}) => {
    return (
        <>
        <div>Product</div>
        <span>{title}</span>
        <span>{description}</span>
        <span>{price}</span>
        </>
    )
}

export {Product}